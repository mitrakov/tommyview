# built with Flutter 2.10.5
name: tommyview
description: A new Flutter project.

version: 23.1.7+2

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^4.6.1             # open-file dialog
  window_manager: ^0.2.7          # set title to window
  image: ^3.1.3                   # image manipulation library
  flutter_platform_alert: ^0.3.0  # platform dialogs
  prompt_dialog: ^1.0.9           # input dialog

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
